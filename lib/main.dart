import 'package:flutter/material.dart';

void main() {
  runApp(AshQuiz());
}

class AshQuiz extends StatelessWidget {
  // This widget is the root of your application.
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
        home: Scaffold(
      backgroundColor: Colors.grey.shade900,
    ));
  }
}
